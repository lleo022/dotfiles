# PATH and Environment Variables
set -gx PATH $HOME/.local/bin $PATH
set -x QT_QPA_PLATFORMTHEME qt6ct

# Optional: Force Fish to use the terminal's palette instead of hex codes
# fish_config theme choose "Default"